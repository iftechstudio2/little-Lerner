function speakText(t){if("speechSynthesis"in window){speechSynthesis.cancel();let u=new SpeechSynthesisUtterance(t);u.lang="en-US";u.rate=.82;u.pitch=1.05;speechSynthesis.speak(u)}else alert("Voice is not supported in this browser.")}function answer(btn,ok){let q=btn.closest(".q"),quiz=btn.closest(".quiz");if(!q||q.dataset.done==="1")return;let f=q.querySelector(".feedback");if(ok){q.dataset.done="1";f.textContent="🎉 Correct! Great job!";f.style.color="#2e7d32";q.querySelectorAll("button").forEach(b=>{b.disabled=true;b.style.opacity=".6"});let s=quiz.querySelector(".score"),n=parseInt(s.textContent)||0;s.textContent=n+1}else{f.textContent="❌ Try Again! You can do it!";f.style.color="#d32f2f"}}function resetQuiz(id){let q=document.getElementById(id);q.querySelectorAll(".q").forEach(x=>{delete x.dataset.done;x.querySelector(".feedback").textContent="";x.querySelectorAll("button").forEach(b=>{b.disabled=false;b.style.opacity="1"})});q.querySelector(".score").textContent="0"}